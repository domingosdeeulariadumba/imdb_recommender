name: Redis Connection Example

on:
  push:
    branches:
      - main

jobs:
  redis-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Display Redis URL from secret
        run: echo "Redis URL: ${{ secrets.REDIS_URL }}"
