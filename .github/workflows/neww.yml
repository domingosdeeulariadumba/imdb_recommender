name: Redis Connection Example

on:
  push:
    branches:
      - main

jobs:
  redis-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Connect to Redis
        run: redis-cli -h ${{ secrets.REDIS_URL }}
